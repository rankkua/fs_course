'''mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: The user writes text into the field and presses the Save button

    Note right of browser: The JavaScript event handler prevents the default form submission and instead creates a new note object

    Note right of browser: The browser adds the new note directly to its own note list and re-renders the notes on the page

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (data: the new note as JSON)
    activate server
    Note right of server: The server saves the new note to the list
    server-->>browser: HTTP 201 (note created)
    deactivate server
'''